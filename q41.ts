let magiciansNames : string [] = ["kiki" , "popo" , "dilji", "samomi"];
function show_magicians(a : string[])
{
    for(let i = 0 ; i< a.length ; i++)
    {
      console.log(a[i]);
    }
}

show_magicians(magiciansNames);
